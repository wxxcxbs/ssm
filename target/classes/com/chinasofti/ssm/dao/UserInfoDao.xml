<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinasofti.ssm.dao.UserInfoDao">
	
	<!-- <insert id="addFileInfo" parameterType="FileInfo">
		insert into file_info(file_name,file_url) values(#{fileName},#{fileUrl})
	</insert> -->
	<select id="getUserInfoList"  resultType="UserInfo">
		select * from user
	</select>
</mapper>